name: example_app
description: A new Flutter project demonstrating the use of social_media_sign_in_buttons package.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  social_media_sign_in_buttons:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true